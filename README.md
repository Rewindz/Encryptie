# Encryptie

Just a simple file encryption program. 

## Build

```
git clone --recursive https://github.com/Rewindz/Encryptie.git
cd Encryptie
cmake . -B build
cd build
cmake --build .
./Encryptie --help
```